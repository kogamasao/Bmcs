/*
 * メンバー追加のヘルプ本文を、複数人をまとめて登録する画面に合わせて書き直す
 *
 * 背景：
 *   メンバー追加を、1人ずつの登録から、1行に1人ずつ入力してまとめて登録する画面に変えた（2026-09-26、issues.md D-1）。
 *   入力項目からメッセージを外し（登録後に編集で入力する）、行の追加・削除を加えたため、ヘルプを合わせる。
 *   見出しも、画面のタイトル（メンバー追加）に合わせる（以前は「メンバー作成」）。
 *
 * 本文は DbInitializer.cs（新規DBの初期データ）と同じ内容にしている。
 * 対象：10 メンバー追加（MemberCreate）
 * 再実行しても安全。GO は使用していない。アプリのデプロイの前後どちらに実行してもよい（本文だけの変更のため）。
 */

--------------------------------------------------------------------------------
-- 1) 変更前の内容を退避する（戻せるようにしておく）
--------------------------------------------------------------------------------
IF OBJECT_ID('dbo.SystemAdminBackup_20260926') IS NULL
BEGIN
    SELECT SystemAdminClass
         , MessageTitle
         , MessageDetail
         , BackupDatetime = SYSDATETIME()
      INTO dbo.SystemAdminBackup_20260926
      FROM dbo.SystemAdmin
     WHERE SystemAdminClass IN (10);
END;

--------------------------------------------------------------------------------
-- 2) 置き換え
--------------------------------------------------------------------------------
-- 10 メンバー追加（MemberCreate）
UPDATE dbo.SystemAdmin
   SET MessageTitle = N'メンバー追加'
     , MessageDetail = N'<p>試合に出る監督や選手を登録します。1行に1人ずつ入力し、チーム全員をまとめて登録できます。登録したメンバーは試合に出場できます。</p><ul class="help-ul"><li>背番号<br>数字3桁以内で入力します。未定の場合は空のままで構いません。</li><li>名前(必須)<br>入力が必要なのは名前だけです。名前も背番号も空の行は登録されません。</li><li>区分<br>選手・監督などの区分です。はじめは「選手」になっています。監督やコーチでも試合に出場できます。</li><li>投・打・ポジション<br>任意です。登録していないポジションでも出場できます。</li><li>行を追加<br>「＋5人分の行を追加」で入力する行を増やせます。1回に50人まで登録できます。不要な行は右端の「×」で削除できます。</li></ul><p>メッセージ（紹介文など）は、登録後にメンバー一覧の「編集」から入力できます。<br>チームIDで参加したチームメイトは「ユーザ」としてスコア入力ができるようになりますが、選手としては登録されません。</p>'
     , UpdateDatetime = SYSDATETIME()
 WHERE SystemAdminClass = 10;

--------------------------------------------------------------------------------
-- 3) 確認
--------------------------------------------------------------------------------
SELECT SystemAdminClass, MessageTitle, LEFT(MessageDetail, 80) AS MessageDetail
  FROM dbo.SystemAdmin
 WHERE SystemAdminClass IN (10);
